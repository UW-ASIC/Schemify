# GitBlame — Schemify C plugin
#
# Build native: make
# Build WASM:   make web    (note: popen/git unavailable in WASM)
# Install:      make install

PLUGIN_NAME = GitBlame
PLUGIN_SRC  = src/plugin.c

include ../../tools/api/c/Makefile
