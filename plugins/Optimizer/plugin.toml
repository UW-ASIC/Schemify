[plugin]
name        = "Optimizer"
version     = "0.1.0"
author      = "UWASIC"
entry       = "libOptimizer.so"
description = "Bayesian circuit optimization plugin with iterative sweep controls."
tags        = ["optimization", "bayesian", "analog"]
