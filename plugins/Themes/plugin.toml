[plugin]
name        = "Themes"
version     = "0.1.0"
author      = "UWASIC"
entry       = "libThemes.so"
description = "Live theme switcher for Schemify. Ships with 7 built-in color themes (dark and light) and supports user-defined themes via ~/.config/Schemify/themes/*.json."
tags        = ["ui", "themes", "colors", "customization"]

[build]
apt_deps = []
