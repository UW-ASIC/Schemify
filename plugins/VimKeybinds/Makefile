# VimKeybinds — Schemify C plugin
#
# Build native: make
# Build WASM:   make web
# Install:      make install

PLUGIN_NAME = VimKeybinds
PLUGIN_SRC  = src/plugin.c

include ../../tools/api/c/Makefile
