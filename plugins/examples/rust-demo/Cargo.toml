[package]
name    = "rust-demo"
version = "0.1.0"
edition = "2021"

[lib]
name       = "rust_demo"
crate-type = ["cdylib"]

[dependencies]
schemify = { path = "../../../tools/api/rust", package = "schemify-plugin" }

[profile.release]
opt-level = "s"
lto       = true
panic     = "abort"
