# Schemify Plugin — Python demo
#
# Build native: make
# Build WASM:   make web
# Install:      make install
#
# Requires: python3-dev
# WASM requires: emscripten + cpython-wasm (set CPYTHON_WASM)

PLUGIN_NAME    = python-demo
PLUGIN_VERSION = 0.1.0
PLUGIN_MODULE  = plugin
PLUGIN_SCRIPT  = plugin.py

include ../../../tools/api/python/Makefile
