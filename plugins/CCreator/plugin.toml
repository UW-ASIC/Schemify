[plugin]
name        = "CCreator"
version     = "0.2.0"
author      = "UWASIC"
entry       = "libCCreator.so"
description = "Circuit toolkit: define PDK-agnostic circuits with templates, behavioral modeling, and circuit generation. Export as Python generators, SPICE, or Verilog-A."
tags        = ["analog", "circuit", "simulation", "testbench", "templates", "behavioral", "generation"]
scope       = "user"

[build]
apt_deps = []
