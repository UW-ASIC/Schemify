[package]
name = "pdk-mapper-plugin"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "pdk-mapper"
path = "src/main.rs"

[dependencies]
schemify-plugins = { path = "../../crates/plugins" }
serde_json = "1"
toml = "0.8"
dirs = "6"

[profile.release]
opt-level = 2
