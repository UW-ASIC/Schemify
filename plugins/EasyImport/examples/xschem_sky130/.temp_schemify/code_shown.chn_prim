chn_prim 1.0

SYMBOL code_shown

  pins [0]:

  params [3]:
    type = netlist_commands
    template = name=s1 only_toplevel=false value=blabla
    format =  @value 

  drawing:
    line 0 -10 70 -10
    line 0 -10 0 10
