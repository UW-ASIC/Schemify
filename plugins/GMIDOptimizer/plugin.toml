[plugin]
name        = "GMIDOptimizer"
version     = "0.1.0"
author      = "UWASIC"
entry       = "libGMIDOptimizer.so"
description = "Bayesian gm/ID optimization: size MOSFETs by optimizing gm/Id ratios with GP surrogate and Expected Improvement acquisition."
tags        = ["analog", "optimization", "gm/id", "bayesian", "mosfet"]
scope       = "user"

[build]
apt_deps = []
