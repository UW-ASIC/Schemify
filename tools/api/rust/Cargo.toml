[package]
name    = "schemify-plugin"
version = "0.1.0"
edition = "2021"
description = "Schemify Plugin SDK — Rust bindings (ABI v6)"

[lib]
crate-type = ["lib"]

[dependencies]
# None — pure Rust, no external crates.

[profile.release]
opt-level = "s"
lto       = true
panic     = "abort"
