# agent

MCP (Model Context Protocol) server for AI-assisted circuit design. Exposes Schematic state and mutation tools to LLM clients over a Unix domain socket via JSON-RPC 2.0. The LLM is the agent; this module is the server.
