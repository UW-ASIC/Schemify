//! gui/types.zig -- shared GUI-layer types.
//!
//! These are types used across multiple gui/ sub-modules that are too
//! GUI-specific to live in state/ and too cross-cutting for a single subdir.
//!
//! Currently empty — populated as types are extracted from sub-modules.
