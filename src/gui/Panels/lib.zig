//! gui/Panels -- floating panel and overlay widgets.
//!
//! Each file exposes a single `draw(app: *AppState) void` entry point.

pub const LibraryBrowser = @import("LibraryBrowser.zig");
pub const Marketplace = @import("Marketplace.zig");
pub const FileExplorer = @import("FileExplorer.zig");
pub const ContextMenu = @import("ContextMenu.zig");
