{
  "name": "schemify-docs",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun x sass style/main.scss style/main.css && bun run build.ts"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "marked-highlight": "^2.1.1",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "sass": "^1.77.0"
  }
}
