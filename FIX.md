1. GUI text is large, needs to be pinned to size, doesn't scale.
   - Add property called show to each element with properties.
     - If has properties then it will be showing the other properties as text beside it pinned.
   - Ipin, Opin, IOPin need a redesign, they look horrindous.

2. The button on the renderer called Spice Code.
   - What does it do? Genuine Question?

3. the button on the toolbar, Simulate -> Ngspice, Xyce, doesn't open up a terminal and run the current whatever screen i'm on's netlist through the simulator. Nothing happens

4. GUI, on the tabbar, the actual tab should have a close button on its left to close it. Thank you.

5. Text/lab pin text shoulnd't be blue, not same color as wires. Must be different.

6. When I left click and move my mouse, it shouldn't move screen. Only when I press spacebar and move mouse (that's panning mode).
   - Instead it should have a select box to select multiple elements and every mode start until I click off will affect all these elements.

7. The plugins/ Marketplace doesn't show anything. Maybe pre-load it even before click. THank you :)
